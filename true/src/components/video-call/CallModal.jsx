import React from 'react';

const CallModal = ({ call, onAccept, onReject }) => {
  // ... (use the exact CallModal.jsx code with Tailwind CSS)
  // Copy the full CallModal component code here
};

export default CallModal;